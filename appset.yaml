apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argocd-root
spec:
  generators:
  - list:
      elements:
      - name: nginx
        stage: test
        targetRevision: HEAD
  template:
    metadata:
      name: '{{stage}}-{{name}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/mrjerzy/argocd-root.git
        targetRevision: HEAD
        path: deployment/{{name}}/stages/{{stage}}
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: '{{stage}}-{{name}}'
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
